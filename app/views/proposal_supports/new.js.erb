$('body').append("<%= escape_javascript(render(:partial => 'proposal_supports/panel'))%>");

$('#proposal_support').dialog({modal:true,
									height:200,
									resizable: true,
									width:420,
	 close: function(event, ui){ 
                                  $(this).dialog('destroy'); 
                                  $('#proposal_support').remove();
               }
});

disegnaBottoni();
