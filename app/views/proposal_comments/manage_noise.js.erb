var container_ = $('<div id="noise_manage_panel" class="reveal-modal medium" data-reveal></div>');
container_.html("<%=j render 'manage_noise'%>");
$('body').append(container_);
airesis_reveal(container_);