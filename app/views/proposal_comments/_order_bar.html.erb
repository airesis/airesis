<%
pv = params[:view] ? params[:view].to_i : nil
if (!pv || (pv == 1))%>
	Random stack
<%else%>	
	<%=link_to 'Random stack', list_proposal_proposal_comments_url(@proposal, :view=>1, :reset => true), :remote => true, :method => :post, :onclick => "hideContributes(1);" %>
<%end%> | 		
<% if (pv == 2)%>
	Più recenti
<%else%>
	<%=link_to 'Più recenti', list_proposal_proposal_comments_url(@proposal, :view=>2, :reset => true), :remote => true, :method => :post, :onclick => "hideContributes(2);" %>	
<%end%> |
<% if (pv == 3)%>
	Più valutati
<%else%>	 
	<%=link_to 'Più valutati', list_proposal_proposal_comments_url(@proposal, :view=>3, :reset => true), :remote => true, :method => :post, :onclick => "hideContributes(3);" %>
<%end%>

<script type="text/javascript">
	function hideContributes(view) {
		checkActive = false;
		currentPage = 1;
		currentView = view;
		$('#loading_contributes').show();
		$('#proposalCommentsContainer').html("");
	}
</script>
