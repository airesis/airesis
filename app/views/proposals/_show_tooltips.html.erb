<%if can? :show_tooltips, current_user %>

    $('#random_stack_l').qtip({
        content: "<%=raw t('tooltips.random_stack_1')%>",
        style: {
          classes: 'qtip-yellow qtip-shadow'
        }
    });

    $('#random_stack_2').qtip({
        content: "<%=raw t('tooltips.random_stack_2')%>",
        style: {
          classes: 'qtip-yellow qtip-shadow'
        }
    });

    $('#random_stack_3').qtip({
        content: "<%=raw t('tooltips.random_stack_3')%>",
        style: {
          classes: 'qtip-yellow qtip-shadow'
        }
    });

    $('#miglioricontributi').qtip({
        content: "<%=raw t('tooltips.miglioricontributi')%>",
        style: {
          classes: 'qtip-yellow qtip-shadow'
        }
    });

    $('#voglioredigere a').qtip({
      content: "<%=raw t('tooltips.voglioredigere') %>",
      style: {
        classes: 'qtip-yellow qtip-shadow'
      }
    });

    $('#voteup').qtip({
     content: "<%=raw t('tooltips.voteup')%>",
     style: {
       classes: 'qtip-yellow qtip-shadow'
     }
    });

    $('#votedown').qtip({
      content: "<%=raw t('tooltips.votedown')%>",
      style: {
        classes: 'qtip-yellow qtip-shadow'
      }
    });

    $('#valutazioni').qtip({
        content: "<%=raw t('tooltips.valutazioni')%>",
        style: {
          classes: 'qtip-yellow qtip-shadow'
        }
    });
    $('#gradimento').qtip({
        content: "<%=raw t('tooltips.gradimento')%>",
        style: {
          classes: 'qtip-yellow qtip-shadow'
        }
    });
    $('#nickname_panel').qtip({
        content: "<%=raw t('tooltips.nickname')%>",
        style: {
          classes: 'qtip-yellow qtip-shadow'
        }
    });
    <%if current_user && (@proposal.users.include? current_user) %>
        $('#noise_comments a').qtip({
            content: "<%=raw t('tooltips.manage_noise')%>",
            style: {
              classes: 'qtip-yellow qtip-shadow'
            }
        });
    <%else%>
        $('#noise_comments a').qtip({
            content: "<%=raw t('tooltips.view_noise')%>",
            style: {
              classes: 'qtip-yellow qtip-shadow'
            }
        });
    <%end%>

<%end%>
<%unless @proposal.is_polling?%>
    $('#suffgrad, #pb<%=@proposal.id%>').qtip({
        content: "<%=@proposal.quorum.explanation%>",
        style: {
          classes: 'qtip-yellow qtip-shadow'
        }
    });
<%end%>