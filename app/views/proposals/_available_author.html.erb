<%unless @proposal.users.include?(current_user) %>
	<%if @proposal.available_user_authors.include?(current_user)%>
        Ti sei offerto per redigere la sintesi della proposta
    <%else%>
        <%=link_to 'Voglio redigere la sintesi!', available_author_proposal_path(@proposal), :remote => true, :method => :post, :class=> 'buttonStyle littleText disableButton'%>
	<%end%>
<%end%>