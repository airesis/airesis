$('#suggestions').empty();

<%if @similars.empty?%>
  var skippedSuggestionStep = true
  $('#form-wizard-next').click()
<%else%>
  var skippedSuggestionStep = false
  $('#suggestions').html("<%= escape_javascript(render 'similar')%>");
<%end%>
