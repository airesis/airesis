<% if @proposalcategories.length == 1 %>
<a href="<%=current_url(:category => nil)%>" class="thistab">Mostra tutte le categorie</a>
<% end %>
<% @proposalcategories.each do |proposalcategory|
proposals = proposalcategory.proposals
proposalsnumber = proposals.length %>
<div id="inner" style="overflow:hidden;border-top: 1px solid #D8D2C3;border-bottom: 1px solid #D8D2C3;">
    <div style="float:left;height:50px">
        <h2><a href="<%=current_url(:category => proposalcategory.description)%>" class="thistab"><%=proposalcategory.description %></a></h2>
    </div>
    <div style="float:left; vertical-align:middle;height:50px">
        contiene <%=h proposalsnumber %>proposte.
    </div>
</div>
<%if proposalsnumber > 0 %>
<% proposals.each do |proposal| %>
<div id="proposal<%= proposal.id %>" class="blogComment">
    <div class="presentation-baloon">
        <table style="padding:5px">
            <body>
                <tr>
                    <td>
                        <div class="blogUserImage">
                            <%= proposal.user_image_tag 50 %>
                        </div>
                        <td>
                            <div id="proposalcc" style="width:680px; float:left">
                                <div class="baloon_header">
                                    <div class="blogCommentPostedAt">
                                        <h3><%= link_to proposal.title, proposal %></h3>
                                        Inserita il <span class="blogPostDate"><%= proposal.created_at.strftime("%d/%m/%Y %H:%M") %></span>
                                        da <span class="blogPoster"><%= proposal.user.link_to_page %></span>
                                        <%if proposal.updated_at != nil %>
                                        (Ultima modifica del <%= proposal.updated_at.strftime("%d/%m/%Y %H:%M") %>)<%end %>
                                        <a class="spch-bub-inside" href="/proposals/<%=proposal.id%>#proposalComments"><span class="point"></span><em><%=proposal.comments.count %></em></a>
                                    </div>
                                </div>
                            </div>
                        </td>
                    </td>
                </tr>
                <tr>
                    <td>
                        <%= render :partial => 'fragments/votes_and_rank', :locals => {:object => proposal} %>
                    </td>
                    <td>
                    <div class="baloon-content">
                        <%=h proposal.content %>
                    </div>
                    <div class="baloon-footer">
                        <%= link_to 'Modifica', edit_proposal_path(proposal) if (is_proprietary? proposal) %>
                    </div>
                    </div>
                </td>
                <td>
                    <div id="rankingpanelcontainer" style="width:40px; float:left;">
                    </div>
                </td>
                </tr>
            </body>
        </table>
    </div>
</div>
<div>
</div>
<% end %>
</table>
<%end %>
<% end %>
