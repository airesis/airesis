{<% if @category%>
    "category": <%=raw @category.to_json({only: [:id, :description]})%>,
<%end %>
<% if @group_area %>
    "group_area": <%=raw @group_area.to_json({only: [:id, :name]})%>,
<%end%>
<% if @group %>
    "group": <%=raw @group.to_json({only: [:id, :name, :image_url]})%>,
<%end%>
    "debate": {
      "id": <%=ProposalState::TAB_DEBATE%>,
      "count": <%=@in_valutation_count%>
    },
    "votation": {
      "id": <%=ProposalState::TAB_VOTATION%>,
      "count":<%=@in_votation_count%>
    },
    "voted": {
      "id": <%=ProposalState::TAB_VOTED%>,
      "count": <%=@accepted_count%>
    },
    "revision": {
      "id": <%=ProposalState::TAB_REVISION%>,
      "count": <%=@revision_count%>
    }
}