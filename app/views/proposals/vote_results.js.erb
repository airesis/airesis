$('#vote_results_container').html("<%=escape_javascript(render(:partial => @proposal.is_schulze? ? 'schulze_vote_results' : 'vote_results'))%>");
$('#vote_results_container').dialog({ width: 500, height: 'auto', modal: true});
disegnaBottoni();
$('#resultsButton').click(function(e) {

  $('#vote_results_container').dialog('open');
    e.preventDefault();
  return false;
})
