<h1>Modifica il gruppo</h1>


<% if @group.errors.any? %>
  <ul>
    <% @group.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>
<% end %>

<%= render :partial => @group, :locals => { :button_name => "Aggiorna" } %>

<%= link_to 'Torna all''elenco', groups_path %>