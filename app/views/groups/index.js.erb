$('#groups_list').fadeOut('slow',function() {
	$('#groups_list').html("<%= escape_javascript(render :partial => "group_tag", :collection => @groups, :as => 'group')%>");
	disegnaBottoni();
	$('#groups_list').fadeIn();	
})
