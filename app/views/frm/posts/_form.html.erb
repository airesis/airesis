<%= f.input :text %>
<%= f.input :tags_list %>
<% if params[:reply_to_id] %>
  <%= f.hidden_field :reply_to_id, :value => params[:reply_to_id] %>
<% end %>


<script type="text/javascript">
    tinyMCE.init({
        mode: 'textareas',
        theme: 'advanced',
        height: 180,
        theme_advanced_toolbar_location: 'top',
        theme_advanced_toolbar_align: 'left',
        theme_advanced_buttons1: 'bold,italic,underline,separator,undo,redo,separator,bullist,numlist,separator,link,unlink,anchor,image,youtube,separator,airesis_emotions',
        theme_advanced_buttons2: '',
        theme_advanced_buttons3: '',
        plugins: 'legacyoutput,media,paste,inlinepopups,youtube,airesis_emotions',
        formats: {
            underline: { inline: 'u' }
        },
        paste_auto_cleanup_on_paste: true,
        theme_advanced_statusbar_location: "none",
        cleanup_on_startup: true,
        relative_urls: false
    });


    function fillHiddenTextarea() {
        $('#frm_topic_posts_attributes_0_text').val(tinyMCE.get('frm_topic_posts_attributes_0_text').getContent());
    }
</script>

