<%@page_title = "#{@group.name} - #{t('frm.admin.area')}" %>
<h1><%= t('frm.admin.area') %></h1>

<br/>

<%= link_to t('frm.admin.category.index'), group_frm_admin_categories_url(@group), class: 'btn blue' %>
<%= link_to t('frm.admin.forum.index'), group_frm_admin_forums_url(@group), class: 'btn blue' %>
<%= link_to t('frm.admin.group.index'), group_frm_admin_frm_groups_url(@group), class: 'btn blue' %> <br/>

<hr/>
<div id='admin_link'>
  <%= link_to t('back_to_forums'), group_forums_url(@group), class: 'btn forwardButton' %>
</div>


<%= content_for :left_panel do %>
    <%= render partial: 'groups/administration_panel' %>
<% end %>
