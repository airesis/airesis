<% if @frm_mod.members.any? %>
    <label><%= t('.current_members') %></label>
    <div class="row">
      <ul id='members'>
        <% @frm_mod.members.each do |member| %>
            <%=user_tag member %>
        <% end %>
      </ul>
    </div>
<% end %>
