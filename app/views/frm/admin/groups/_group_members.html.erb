<% if @frm_group.members.any? %>
    <label><%= t('.current_members') %></label>
    <div class="row">
      <ul id='members'>
        <% @frm_group.members.each do |member| %>
            <%=user_tag member %>
        <% end %>
      </ul>
    </div>
<% end %>