<%= form_for(@search_proposal) do |f| %>
  <% if @search_proposal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@search_proposal.errors.count, "error") %> prohibited this search_proposal from being saved:</h2>

      <ul>
      <% @search_proposal.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
