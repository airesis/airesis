$('#new_group_area_container').html("<%= escape_javascript(render(:partial => 'group_areas/form'))%>");

$('#group_area_panel').dialog({
	width: 500,
	height: 'auto',
	modal : true,
	resizable: false,
	close : function(event, ui) {
		$('#group_area_panel').dialog('destroy');
	}
});

$("#new_group_area").enableClientSideValidations();

