$('#edit_role').html("<%= escape_javascript(render(:partial => 'partecipation_roles/form', :locals => {:button_name => "Aggiorna"}))%>");

$('#edit_role').dialog({
	width: 330,
	height: 'auto',
	modal : true,
	resizable: false,
	close : function(event, ui) {
		$('#edit_role').dialog('destroy');
	}
});

