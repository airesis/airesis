var container_ = $('<div id="partecipation_role_container" class="reveal-modal small" data-reveal></div>');
container_.html("<%= j (render(partial: 'partecipation_roles/form', locals: {button_name: t('buttons.update')}))%>");
$('body').append(container_);
airesis_reveal(container_);