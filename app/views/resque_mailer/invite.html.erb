Ciao!<br/>
<%=@group_invitation.inviter.fullname %> ti ha invitato a partecipare al gruppo
<b><%=link_to @group.name, group_url(@group)%></b> su <%= link_to APP_LONG_NAME, Maktoub.home_domain%>,
la piattaforma decisionale per la democrazia partecipata e diretta.<br />
<%if @group_invitation.testo %>
Ecco il messaggio che ti ha inviato:<br/>
    <p style="font-style: italic;">
      <%=h @group_invitation.testo%>
    </p>
<%end%>

Se accetti l'invito clicca su 'Accetto' altrimenti su 'Rifiuta'. <br />
Se non desideri più ricevere inviti da parte di <%= link_to APP_SHORT_NAME, Maktoub.home_domain%> clicca su 'Non ricevere più inviti'.<br/>

<%= link_to 'Accetta', accept_group_invitations_url(:group_id => @group.id, :token => @group_invitation.token, :email => @group_invitation_email.email), style: 'text-decoration: none;
        text-shadow: 0 1px rgba(0, 0, 0, 0.1);font: bold 11px Arial, sans-serif;color: #444;
        line-height: 17px;
        display: inline-block;
        margin: 5px;
        padding: 8px 14px;
        background: #F3F3F3;
        border: solid 1px #D9D9D9;
        border-radius: 2px;
        -webkit-border-radius: 2px;
        -moz-border-radius: 2px;
        -webkit-transition: border-color .20s;
        -moz-transition: border-color .20s;
        -o-transition: border-color .20s;
        transition: border-color .20s;
        cursor: pointer;
        position: relative;' %> |
<%= link_to 'Rifiuta', reject_group_invitations_url(:group_id => @group.id, :token => @group_invitation.token, :email => @group_invitation_email.email), style: 'text-decoration: none;
        text-shadow: 0 1px rgba(0, 0, 0, 0.1);font: bold 11px Arial, sans-serif;color: #444;line-height: 17px;display: inline-block;margin: 5px;padding: 8px 14px;background: #F3F3F3;
        border: solid 1px #D9D9D9;
        border-radius: 2px;
        -webkit-border-radius: 2px;
        -moz-border-radius: 2px;
        -webkit-transition: border-color .20s;
        -moz-transition: border-color .20s;
        -o-transition: border-color .20s;
        transition: border-color .20s;
        cursor: pointer;
        position: relative;' %> <br />
<br />
A presto!<br />
<%= link_to 'Non ricevere più inviti', anymore_group_invitations_url(:group_id => @group.id, :token => @group_invitation.token, :email => @group_invitation_email.email), :style=> "font-size: 10px;" %>
