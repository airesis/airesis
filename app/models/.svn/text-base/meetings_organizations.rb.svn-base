class MeetingsOrganization < ActiveRecord::Base
  belongs_to :groups, :class_name => 'Group', :foreign_key => :group_id
  belongs_to :meetings, :class_name => 'Meeting', :foreign_key => :meeting_id
end
