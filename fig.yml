db:
  image: postgres
  ports:
    - "5432"
redis:
  image: redis
  ports:
    - "6379"
web:
  build: .
  environment:
      RAILS_ENV: production
  command: foreman start
  volumes:
    - .:/app
  ports:
    - "80:80"
  links:
    - db
    - redis
